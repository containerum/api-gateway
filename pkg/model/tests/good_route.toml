[routes]
[routes.test_route]
active = true
name = "Test route"
roles = ["*"]
method = "GET"
upstream = "http://localhost:80"
listen = "/test"
strip = false
group = "user"

[routes.test_route_2]
active = true
name = "Test route"
roles = ["*"]
method = "GET"
upstream = "http://localhost:80"
listen = "/test"
strip = false
group = "user"
